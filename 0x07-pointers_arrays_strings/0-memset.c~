#include "holberton.h"
/**
 * _memset - fill memory with a constant byte
 * @s: first digit
 * @b: second digit
 * @n; third digit
 * Return: 0
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int i = 0;
while (i < n)
{
s[i] = b;
i++;
}
return (s);
}
