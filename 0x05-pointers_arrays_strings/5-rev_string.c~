#include "holberton.h"
/**
 * rev_string - reverse a string
 *  @s : number one
 * Return : 0
 */
void rev_string(char *s)
{
int i = 0;
int length = strlen(s);
for (i < length/2; i++)
char temp = s[i];
s[i] = s[length-1-i];
s[length-1-i] = temp;
}
