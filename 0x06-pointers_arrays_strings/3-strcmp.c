#include "holberton.h"
/**
 * _strcmp - compares two strings
 * @s1: number one
 * @s2: number two
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
int flag = 0;
while (s1 != '\0' || s2 != '\0')
|| (s1 != '\0' && s2 == '\0')
	|| (s1 != s2)
	{
	flag = 1;
	}
return (0);
}
